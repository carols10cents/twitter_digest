require 'spec_helper'

describe User do
  describe "#settings" do
    it "has newest first for initial timeline order"
    it "has oldest first for initial conversation order"
    it "can change the prefs"
      
  end
end
