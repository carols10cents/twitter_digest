<div class="stats">
  Viewing <%= @num_unabridged_tweets %> tweets since you last checked;
  <%= @num_in_conversations %> are in conversations.
</div>
<ul class='tweets'>
  <% if !@digested_tweets.stream.empty? %>
    <% @digested_tweets.stream.each do |tweet_or_conversation| %>
      <%= render(tweet_or_conversation) %>
    <% end %>
  <% else %>
    <li>No tweets since last time you checked.</li>
  <% end %>
</ul>